name: Robo do Diario Magico

on:
  schedule:
    - cron: '0 12 * * *'   # 12:00 UTC = 09:00 em Brasília
  workflow_dispatch:

jobs:
  postar:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar dependências
        run: pip install requests groq google-api-python-client google-auth-oauthlib google-auth

      - name: Rodar robô do diário mágico
        run: python diario_magico.py
        env:
          GROQ_API_KEY:           ${{ secrets.GROQ_API_KEY }}
          BLOGGER_ID_MAGO:        ${{ secrets.BLOGGER_ID_MAGO }}
          BLOGGER_CLIENT_ID:      ${{ secrets.BLOGGER_CLIENT_ID }}
          BLOGGER_CLIENT_SECRET:  ${{ secrets.BLOGGER_CLIENT_SECRET }}
          BLOGGER_REFRESH_TOKEN:  ${{ secrets.BLOGGER_REFRESH_TOKEN }}
          # Token opcional do Pollinations.ai para geração de imagens (fallback: Openverse)
          POLLINATIONS_TOKEN:     ${{ secrets.POLLINATIONS_TOKEN }}
          IMGBB_API_KEY:          ${{ secrets.IMGBB_API_KEY }}

      - name: Salvar estado e histórico
        run: |
          git config user.name "Robo do Diario Magico"
          git config user.email "robo@exemplo.com"
          git add estado_mago.json historico_magico.txt || true
          git commit -m "Atualiza estado e historico do diario magico" || true
          git push || true
